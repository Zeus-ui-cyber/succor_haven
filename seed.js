const bcrypt=require("bcrypt"),{Pool}=require("pg");require("dotenv").config();const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});bcrypt.hash("admin1234",10).then(h=>pool.query("INSERT INTO users (email,password_hash,first_name,last_name,role) VALUES ($1,$2,$3,$4,$5) ON CONFLICT (email) DO UPDATE SET password_hash=$2",["admin@succorhaven.com",h,"Admin","User","admin"])).then(()=>{console.log("? Admin created");pool.end()}).catch(e=>{console.error(e.message);pool.end()});
